// App-wide configuration values

export const DEFAULT_PAGE_SIZE = 25;

// Placeholder para auditoría hasta que se integre el sistema de usuarios.
// Cuando se integre OpenMRS u otro proveedor de identidad, reemplazar por
// el UUID del usuario autenticado extraído del token/request.
export const CREATED_BY_PLACEHOLDER: string = 'system';
